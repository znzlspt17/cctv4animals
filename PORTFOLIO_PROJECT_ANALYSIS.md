# 포트폴리오용 프로젝트 분석 정리

## 문서 목적
- ChatGPT 등 생성형 AI 서비스에 PPT 제작을 요청할 때 바로 활용할 수 있도록 프로젝트의 **기획 의도**, **핵심 기능**, **기술 스택**, **향후 통합 방향**을 한 문서로 정리한다.

## 분석 범위
- 현재 저장소에서 확인 가능한 자료만 기반으로 정리했다.
- 확인한 주요 파일:
  - `README.md`
  - `PROJECT_STATUS.md`
  - `pyproject.toml`
  - `server/main.py`
  - `app.py`
  - `server/services/plant/lettuce_service.py`
- **주의:** 현재 저장소 안에서는 `img2cad`, `WT_FINAL_PJ`라는 이름의 별도 디렉터리/문서/코드를 확인하지 못했다.
- 따라서 아래 내용은 **현재 저장소 프로젝트(cctv4animals, 문서상 서비스명: DeepFace Live)** 를 중심으로 정리했고, `img2cad`는 **사용자 설명으로만 확인된 향후 통합 대상 프로젝트** 로 명시한다.

---

## 1. 프로젝트 A 분석 — 현재 저장소 프로젝트 (cctv4animals / 서비스명 DeepFace Live)

### 1-1. 프로젝트 한 줄 소개
- CCTV 기반 영상 데이터를 활용해 **얼굴 인식, 동물 탐지, 일반 식물 병해 탐지, 상추 특화 질병 탐지**를 수행하고, 결과를 저장·조회·전송하는 **실시간 AI 엣지 서버 통합 시스템**이다.

### 1-2. 기획 의도
- 하나의 시스템 안에서 여러 AI 기능을 통합해 **현장형 스마트 관제/모니터링 서비스**를 구현하는 것이 핵심 목적이다.
- 단순 추론에 그치지 않고, 추론 결과를 **DB에 기록**하고 **외부 서버로 전달**해 실제 운영 시스템과 연결 가능한 구조를 목표로 한다.
- 관리자 UI와 API 서버를 함께 두어 **테스트·검증·운영 전환**이 쉬운 구조를 지향한다.
- 향후 다른 AI 프로젝트(`img2cad`)와 결합해 **도메인 확장형 통합 플랫폼**으로 발전시키기 좋은 기반을 만든 프로젝트로 볼 수 있다.

### 1-3. 해결하려는 문제
- CCTV나 이미지 기반 현장 데이터를 사람이 직접 확인해야 하는 비효율
- 얼굴/동물/식물 탐지를 각각 별도 시스템으로 운영할 때 발생하는 관리 복잡도
- 탐지 결과가 화면 표시에서 끝나고 저장/전송/후처리로 연결되지 않는 문제
- AI 모델 검증과 운영 UI가 분리되어 개발 생산성이 떨어지는 문제

### 1-4. 핵심 기능
- 얼굴 등록 및 얼굴 인식
- 동물 객체 탐지
- 일반 식물 병해 탐지
- 상추 특화 질병 탐지
- 탐지 결과 DB 저장
- 외부 서버 이벤트 전송
- 관리자용 Streamlit 테스트 UI 제공
- FastAPI 기반 REST API 제공

### 1-5. 기술 스택

#### 백엔드/API
- Python 3.12+
- FastAPI
- Uvicorn
- Pydantic
- pydantic-settings
- python-multipart

#### 프론트/UI
- Streamlit
- streamlit-webrtc
- streamlit-drawable-canvas

#### AI/머신러닝
- PyTorch
- torchvision
- Ultralytics YOLO
- DeepFace
- ArcFace
- RetinaFace
- TensorFlow / tf-keras
- FAISS
- OpenCV
- Pillow
- NumPy

#### 데이터베이스
- PostgreSQL
- SQLAlchemy
- psycopg2-binary
- pgvector

#### 영상/미디어 처리
- PyAV
- OpenCV

#### 개발/테스트
- pytest
- httpx
- Jupyter Notebook

#### 패키지/실행 환경
- uv

### 1-6. 사용 모델 및 역할
- **ArcFace + RetinaFace**: 얼굴 임베딩 생성 및 얼굴 인식
- **FAISS IndexFlatIP**: 얼굴 임베딩 유사도 검색
- **YOLO 모델**: 동물 탐지
- **Faster R-CNN (ConvNeXt+FPN 기반)**: 식물 병해 탐지
- **상추 전용 질병 탐지 모델**: 상추 병해 탐지

### 1-7. 아키텍처 관점 요약
- **FastAPI 서버**가 중심이 되어 AI 추론, 데이터 저장, 이벤트 전송을 담당
- **Streamlit 앱**이 테스트용 관리자 UI 역할 수행
- **PostgreSQL + pgvector**가 탐지 로그 및 임베딩 저장 담당
- 각 기능은 서비스 단위로 분리되어 있어 동물/식물/상추 탐지 기능을 독립적으로 확장 가능

### 1-8. 포트폴리오에서 강조하기 좋은 포인트
- 단일 기능 프로젝트가 아니라 **여러 AI 기능을 통합한 플랫폼형 프로젝트**
- 단순 모델 추론이 아니라 **API, UI, DB, 외부 연동**까지 포함한 **실서비스형 구조**
- AI 모델을 실제 운영 흐름에 맞게 **저장·전송·시각화**까지 연결
- 향후 다른 프로젝트를 붙이기 쉬운 **모듈형 구조**

---

## 2. 프로젝트 B 분석 — img2cad

### 2-1. 현재 확인 가능한 내용
- 사용자 설명에 따르면 `img2cad` 프로젝트는 **향후 WT_FINAL_PJ에 통합될 예정인 별도 프로젝트**다.
- 그러나 현재 저장소에서는 `img2cad` 관련 코드, 문서, 디렉터리, 기술 스택 정보를 확인하지 못했다.

### 2-2. 현재 문서에 적을 수 있는 수준의 정리
- **프로젝트 성격:** 별도 프로젝트이며 추후 통합 예정
- **통합 방향:** WT_FINAL_PJ의 기능 확장을 위한 후속 결합 대상
- **기술 스택:** 현재 저장소 기준 확인 불가
- **구체 기능:** 현재 저장소 기준 확인 불가
- **기획 의도:** 현재 저장소 기준 확인 불가

### 2-3. 추가 자료가 필요함
아래 자료가 있어야 `img2cad`를 같은 수준으로 정리할 수 있다.
- img2cad 저장소 경로 또는 링크
- README / 기술 문서
- 사용 모델 또는 핵심 라이브러리 정보
- 주요 기능 화면 또는 API 구조
- 최종적으로 WT_FINAL_PJ에 어떻게 붙일지에 대한 설계 개요

---

## 3. 두 프로젝트의 연결 관점

### 3-1. 현재 기준 해석
- 현재 저장소 프로젝트는 **AI 추론 결과를 처리·저장·전송하는 통합 플랫폼** 역할에 적합하다.
- `img2cad`는 향후 이 플랫폼에 추가될 **별도 AI 기능 모듈**로 이해할 수 있다.

### 3-2. 포트폴리오에서 설명하기 좋은 통합 메시지
- WT_FINAL_PJ는 다양한 AI 기능을 수용할 수 있는 통합 플랫폼으로 설계되었다.
- img2cad는 향후 이 플랫폼에 결합될 확장 기능으로, 프로젝트 전체를 **단일 목적 앱이 아닌 확장형 AI 서비스 플랫폼**으로 발전시키는 역할을 한다.
- 즉, 현재 프로젝트는 **플랫폼**, img2cad는 **추가 도메인 기능 모듈**이라는 관점으로 설명하면 자연스럽다.

---

## 4. PPT 작성용 핵심 요약 문장

### 프로젝트 소개
- CCTV 및 이미지 데이터를 활용해 얼굴 인식, 동물 탐지, 식물 병해 탐지를 수행하고 결과를 저장·전송하는 AI 엣지 서버 통합 시스템을 개발했다.

### 기획 의도
- 여러 AI 기능을 하나의 플랫폼으로 통합해 현장형 모니터링과 후속 데이터 활용이 가능한 구조를 만드는 것이 목표였다.

### 기술적 강점
- FastAPI, Streamlit, PostgreSQL, pgvector, PyTorch, YOLO, DeepFace, FAISS를 결합해 추론부터 저장, 조회, 외부 연동까지 전 과정을 구현했다.

### 확장성
- 향후 img2cad 프로젝트를 통합해 기능 범위를 확장할 수 있는 모듈형 구조를 지향했다.

---

## 5. 발표 자료에 넣기 좋은 키워드
- AI Edge Server
- CCTV 기반 실시간 분석
- 얼굴 인식
- 동물 탐지
- 식물 병해 탐지
- 상추 질병 탐지
- 관리자 대시보드
- REST API
- PostgreSQL + pgvector
- FastAPI + Streamlit
- PyTorch + YOLO + DeepFace
- 확장형 통합 플랫폼

---

## 6. 후속 작업 권장 사항
- `img2cad` 저장소 또는 관련 문서를 추가로 받아 이 문서에 별도 섹션을 보강
- 두 프로젝트를 비교하는 표(목적, 입력 데이터, 출력 결과, 기술 스택, 통합 포인트) 추가
- 최종 PPT용으로는 본 문서에서 **기획 의도 / 기술 스택 / 핵심 기능 / 통합 방향**만 추려 슬라이드화하는 것이 적합
